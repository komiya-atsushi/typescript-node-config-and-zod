{
  "name": "typescript-node-config-and-zod",
  "version": "1.0.0",
  "description": "An example TypeScript project using node-config and zod.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/config": "^3.3.0",
    "@types/node": "^20.4.5",
    "gts": "^5.0.0",
    "typescript": "~5.1.6"
  },
  "dependencies": {
    "config": "^3.3.9",
    "ts-node": "^10.9.1",
    "zod": "^3.22.1"
  }
}
